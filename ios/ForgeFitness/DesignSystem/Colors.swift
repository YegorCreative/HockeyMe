import SwiftUI

enum AppColors {
    static let primary = Color(red: 0.08, green: 0.25, blue: 0.42)
    static let secondary = Color(red: 0.18, green: 0.48, blue: 0.72)
    static let background = Color(.systemBackground)
    static let surface = Color(.secondarySystemBackground)
    static let textPrimary = Color.primary
    static let textSecondary = Color.secondary
    static let border = Color(.separator)
    static let success = Color(red: 0.13, green: 0.55, blue: 0.31)
    static let warning = Color(red: 0.95, green: 0.61, blue: 0.07)
    static let error = Color(red: 0.78, green: 0.16, blue: 0.16)
}
